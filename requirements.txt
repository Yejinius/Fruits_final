# 웹 크롤링
requests==2.31.0
beautifulsoup4==4.12.3
selenium==4.18.1
webdriver-manager==4.0.1

# 데이터베이스
sqlalchemy==2.0.25

# 이미지 처리
Pillow==10.2.0

# 유틸리티
python-dotenv==1.0.1
tqdm==4.66.1

# 웹 프레임워크
flask==3.0.2
flask-limiter==3.5.1

# 프로덕션 WSGI 서버
gunicorn==22.0.0

# Telegram Bot (알림 + 승인 플로우)
# requests는 이미 포함됨 — telegram_bot.py는 requests로 직접 API 호출
